{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd \"../web版/merchant-erp\" && npm ci && cd - && npm ci",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
