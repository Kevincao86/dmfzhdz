墨典商家小程序软件 V1.0
第 1 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
/* ===== FILE: 墨典商家小程序/app.js ===== */
const devAuth = require('./utils/devAuth.js')
const sessionSync = require('./utils/merchantSessionSyncMp.js')

App({
  onLaunch() {
    if (devAuth.isDevSkipLogin()) {
      devAuth.applyDevSession()
      this.globalData.accessToken = devAuth.DEV_TOKEN
      return
    }
    const token = wx.getStorageSync('meoo_access_token')
    if (token) {
      this.globalData.accessToken = token
      void sessionSync.syncFromCloud()
    }
  },
  globalData: {
    accessToken: null,
  },
  /** 与 Web 设置页云端绑定同步（抖音 / 本地推 / 聚光） */
  syncMerchantSession(opts) {
    return sessionSync.syncFromCloud(opts)
  },
  /** 供页面校验登录态 */
  ensureAuthed() {
    if (devAuth.isDevSkipLogin()) {
      devAuth.applyDevSession()
      this.globalData.accessToken = devAuth.DEV_TOKEN
      return true
    }
    const t = wx.getStorageSync('meoo_access_token')
    if (!t) {
      wx.redirectTo({ url: '/pages/login/login' })
      return false
    }
    return true
  },
})


/* ===== FILE: 墨典商家小程序/app.json ===== */
{
  "pages": [
    "pages/agent/agent",
    "pages/functions/functions",
    "pages/mine/mine",
    "pages/login/login",
    "pages/home/home",
    "pages/dashboard/dashboard",

墨典商家小程序软件 V1.0
第 2 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    "pages/module-detail/module-detail",
    "pages/wallet/wallet",
    "pages/recruitment/recruitment",
    "pages/product-list/product-list",
    "pages/reviews-list/reviews-list",
    "pages/product-voice/product-voice",
    "pages/product-edit/product-edit",
    "pages/shortvideo-voice/shortvideo-voice",
    "pages/shortvideo-edit/shortvideo-edit",
    "pages/recruit-voice/recruit-voice",
    "pages/recruit-edit/recruit-edit",
    "pages/product-create/product-create",
    "pages/store-list/store-list",
    "pages/activity-center/activity-center",
    "pages/finance-reconcile/finance-reconcile",
    "pages/finance-tax/finance-tax",
    "pages/ads-manage/ads-manage",
    "pages/leads-center/leads-center",
    "pages/geo-assist/geo-assist",
    "pages/ai-content/ai-content",
    "pages/notifications/notifications",
    "pages/subscription/subscription",
    "pages/support-chat/support-chat"
  ],
  "window": {
    "navigationBarTitleText": "墨典商家",
    "navigationBarBackgroundColor": "#ffffff",
    "navigationBarTextStyle": "black",
    "backgroundColor": "#f8fafc",
    "backgroundTextStyle": "dark"
  },
  "tabBar": {
    "custom": true,
    "color": "#94a3b8",
    "selectedColor": "#0284c7",
    "backgroundColor": "#ffffff",
    "list": [
      {
        "pagePath": "pages/agent/agent",
        "text": "墨典AI"
      },
      {
        "pagePath": "pages/functions/functions",
        "text": "功能"
      },
      {
        "pagePath": "pages/mine/mine",
        "text": "我的"
      }
    ]

墨典商家小程序软件 V1.0
第 3 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  },
  "permission": {
    "scope.record": {
      "desc": "用于智能体语音输入、商品与招募语音录入"
    },
    "scope.camera": {
      "desc": "用于拍摄商品图、评论截图等附件上传"
    }
  },
  "requiredPrivateInfos": [],
  "lazyCodeLoading": "requiredComponents"
}


/* ===== FILE: 墨典商家小程序/app.wxss ===== */
page {
  background: #f3f6fb;
  color: #1e293b;
  font-size: 28rpx;
  box-sizing: border-box;
  --tab-bar-pad: calc(76rpx + env(safe-area-inset-bottom));
  --agent-composer-offset: calc(76rpx + env(safe-area-inset-bottom));
}

/* 功能子页通用列表 */
.mp-page {
  min-height: 100vh;
  background: linear-gradient(180deg, #eef6ff 0%, #f8fafc 30%);
  display: flex;
  flex-direction: column;
}

.mp-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  padding: 20rpx 24rpx 8rpx;
}

.mp-tab {
  padding: 12rpx 22rpx;
  border-radius: 999rpx;
  font-size: 24rpx;
  color: #64748b;
  background: #fff;
  border: 1rpx solid #e2e8f0;
}

.mp-tab.active {
  color: #fff;

墨典商家小程序软件 V1.0
第 4 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  background: linear-gradient(95deg, #0ea5e9, #06b6d4);
  border-color: transparent;
}

.mp-banner {
  margin: 8rpx 24rpx 0;
  padding: 16rpx 20rpx;
  border-radius: 16rpx;
  background: rgba(56, 189, 248, 0.1);
  color: #0369a1;
  font-size: 22rpx;
  line-height: 1.45;
}

.mp-list-scroll {
  flex: 1;
  height: 0;
  padding: 16rpx 24rpx;
  box-sizing: border-box;
}

.mp-list-card {
  margin-bottom: 16rpx;
  padding: 24rpx;
  background: #fff;
  border-radius: 20rpx;
  border: 1rpx solid rgba(15, 23, 42, 0.05);
  box-shadow: 0 6rpx 20rpx rgba(15, 23, 42, 0.04);
}

.mp-list-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #0f172a;
  display: block;
}

.mp-list-sub {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: #475569;
  line-height: 1.45;
  display: block;
}

.mp-list-meta {
  margin-top: 10rpx;
  font-size: 22rpx;
  color: #94a3b8;
  display: block;

墨典商家小程序软件 V1.0
第 5 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

.mp-list-tag {
  display: inline-block;
  margin-top: 10rpx;
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: #f1f5f9;
  color: #475569;
}

.mp-empty,
.mp-err {
  padding: 48rpx 32rpx;
  text-align: center;
  font-size: 26rpx;
  color: #64748b;
}

.mp-err {
  color: #b91c1c;
}

.mp-list-footer {
  height: 32rpx;
}

.mp-form-card {
  margin: 16rpx 24rpx;
  padding: 24rpx;
  background: #fff;
  border-radius: 20rpx;
}

.mp-output {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #f8fafc;
  border-radius: 16rpx;
  font-size: 24rpx;
  color: #334155;
  line-height: 1.55;
  white-space: pre-wrap;
}

.meoo-card {
  background: #ffffff;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
  border-radius: 24rpx;

墨典商家小程序软件 V1.0
第 6 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  padding: 32rpx;
  box-shadow: 0 10rpx 40rpx rgba(15, 23, 42, 0.06);
}

.meoo-btn-primary {
  background: linear-gradient(95deg, #0ea5e9, #06b6d4 45%, #14b8a6);
  color: #fff;
  border-radius: 16rpx;
  padding: 24rpx 32rpx;
  font-weight: 600;
  text-align: center;
  box-shadow: 0 8rpx 24rpx rgba(14, 165, 233, 0.28);
}

.meoo-btn-ghost {
  border: 1rpx solid rgba(148, 163, 184, 0.55);
  color: #475569;
  border-radius: 16rpx;
  padding: 24rpx 32rpx;
  text-align: center;
  background: rgba(255, 255, 255, 0.85);
}

.meoo-input {
  background: #f8fafc;
  border: 1rpx solid rgba(148, 163, 184, 0.45);
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  color: #0f172a;
}

.meoo-label {
  font-size: 22rpx;
  color: #64748b;
  margin-bottom: 12rpx;
  display: block;
}

.meoo-hint {
  font-size: 22rpx;
  color: #64748b;
  line-height: 1.5;
}

/* 语音录入页 */
.voice-page {
  padding: 28rpx;
  padding-bottom: 48rpx;
  box-sizing: border-box;
  min-height: 100vh;

墨典商家小程序软件 V1.0
第 7 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  background: linear-gradient(180deg, #eef6ff 0%, #f3f6fb 35%);
}

.voice-hero {
  margin-bottom: 24rpx;
}

.voice-hero-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
  display: block;
}

.voice-hero-desc {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: #64748b;
  line-height: 1.55;
  display: block;
}

.voice-steps {
  margin-bottom: 28rpx;
  padding: 24rpx 28rpx;
}

.voice-steps-title {
  font-size: 22rpx;
  color: #64748b;
  margin-bottom: 16rpx;
  display: block;
}

.voice-step-row {
  display: flex;
  align-items: flex-start;
  margin-bottom: 14rpx;
}

.voice-step-num {
  width: 40rpx;
  height: 40rpx;
  border-radius: 999rpx;
  background: rgba(14, 165, 233, 0.15);
  color: #0284c7;
  font-size: 22rpx;
  font-weight: 700;
  display: flex;
  align-items: center;

墨典商家小程序软件 V1.0
第 8 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  justify-content: center;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.voice-step-text {
  flex: 1;
  font-size: 24rpx;
  color: #475569;
  line-height: 1.45;
}

.voice-actions {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.voice-record-btn {
  min-height: 96rpx;
  line-height: 1.4;
}

.voice-pulse {
  margin-top: 28rpx;
  text-align: center;
  color: #0284c7;
  font-size: 26rpx;
}

.edit-section-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #0f172a;
  margin: 32rpx 0 16rpx;
  display: block;
}

.edit-page-hint {
  margin-bottom: 24rpx;
}


/* ===== FILE: 墨典商家小程序/custom-tab-bar/index.js ===== */
Component({
  data: {
    selected: 0,
    list: [
      { pagePath: '/pages/agent/agent', text: '墨典AI' },
      { pagePath: '/pages/functions/functions', text: '功能' },

墨典商家小程序软件 V1.0
第 9 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      { pagePath: '/pages/mine/mine', text: '我的' },
    ],
  },
  methods: {
    switchTab(e) {
      const idx = Number(e.currentTarget.dataset.index)
      const item = this.data.list[idx]
      if (!item) return
      wx.switchTab({ url: item.pagePath })
    },
  },
})


/* ===== FILE: 墨典商家小程序/custom-tab-bar/index.json ===== */
{
  "component": true
}


/* ===== FILE: 墨典商家小程序/custom-tab-bar/index.wxml ===== */
<view class="tab-bar">
  <view
    wx:for="{{list}}"
    wx:key="pagePath"
    class="tab-item {{selected === index ? 'active' : ''}}"
    data-index="{{index}}"
    bindtap="switchTab"
  >
    <text class="tab-text">{{item.text}}</text>
  </view>
</view>


/* ===== FILE: 墨典商家小程序/custom-tab-bar/index.wxss ===== */
.tab-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  height: calc(76rpx + env(safe-area-inset-bottom));
  padding-bottom: env(safe-area-inset-bottom);
  display: flex;
  align-items: stretch;
  background: rgba(255, 255, 255, 0.98);
  border-top: 1rpx solid rgba(15, 23, 42, 0.06);
  z-index: 9000;
}

.tab-item {

墨典商家小程序软件 V1.0
第 10 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #94a3b8;
}

.tab-item.active {
  color: #0284c7;
}

.tab-text {
  font-size: 26rpx;
  font-weight: 600;
  letter-spacing: 0.5rpx;
}


/* ===== FILE: 墨典商家小程序/pages/activity-center/activity-center.js ===== */
const api = require('../../utils/api.js')
const feature = require('../../utils/merchantFeatureMp.js')
const { PLATFORM_TABS } = require('../../utils/platformTokensMp.js')

Page({
  data: {
    platform: 'douyin',
    tabs: PLATFORM_TABS.filter((p) => ['douyin', 'meituan', 'xiaohongshu'].includes(p.id)),
    loading: false,
    err: '',
    items: [],
  },
  onShow() {
    if (!api.getAccessToken()) {
      wx.redirectTo({ url: '/pages/login/login' })
      return
    }
    void this.load()
  },
  onTab(e) {
    this.setData({ platform: e.currentTarget.dataset.id })
    void this.load()
  },
  async load() {
    this.setData({ loading: true, err: '' })
    const r = await feature.fetchMarketingActivities(this.data.platform, 'all')
    this.setData({
      loading: false,
      err: r.ok ? '' : r.message,
      items: r.ok ? r.items : [],
    })

墨典商家小程序软件 V1.0
第 11 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  },
})


/* ===== FILE: 墨典商家小程序/pages/activity-center/activity-center.json ===== */
{ "navigationBarTitleText": "活动中心" }


/* ===== FILE: 墨典商家小程序/pages/activity-center/activity-center.wxml ===== */
<view class="mp-page">
  <view class="mp-tabs">
    <view wx:for="{{tabs}}" wx:key="id" class="mp-tab {{platform === item.id ? 'active' : ''}}" data-id="{{item.id}}" bindtap="onTab">{{item.label}}</view>
  </view>
  <view wx:if="{{loading}}" class="mp-empty">加载中…</view>
  <view wx:elif="{{err}}" class="mp-err">{{err}}</view>
  <view wx:elif="{{!items.length}}" class="mp-empty">暂无活动</view>
  <scroll-view wx:else scroll-y class="mp-list-scroll">
    <view wx:for="{{items}}" wx:key="id" class="mp-list-card">
      <text class="mp-list-title">{{item.title}}</text>
      <text class="mp-list-tag">{{item.uiStatusLabel}}</text>
      <text wx:if="{{item.summary}}" class="mp-list-sub">{{item.summary}}</text>
      <text wx:if="{{item.startAt}}" class="mp-list-meta">{{item.startAt}} — {{item.endAt}}</text>
    </view>
    <view class="mp-list-footer"></view>
  </scroll-view>
</view>


/* ===== FILE: 墨典商家小程序/pages/activity-center/activity-center.wxss ===== */


/* ===== FILE: 墨典商家小程序/pages/ads-manage/ads-manage.js ===== */
const api = require('../../utils/api.js')
const feature = require('../../utils/merchantFeatureMp.js')

Page({
  data: { loading: false, err: '', items: [] },
  onShow() {
    if (!api.getAccessToken()) {
      wx.redirectTo({ url: '/pages/login/login' })
      return
    }
    void this.load()
  },
  async load() {
    this.setData({ loading: true, err: '' })
    const r = await feature.fetchLocalPromotions()
    this.setData({
      loading: false,
      err: r.ok ? '' : r.message,

墨典商家小程序软件 V1.0
第 12 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      items: r.ok ? r.items : [],
    })
  },
})


/* ===== FILE: 墨典商家小程序/pages/ads-manage/ads-manage.json ===== */
{ "navigationBarTitleText": "投流管理" }


/* ===== FILE: 墨典商家小程序/pages/ads-manage/ads-manage.wxml ===== */
<view class="mp-page">
  <view class="mp-banner">巨量本地推投放计划，数据与商家后台投流模块同步。</view>
  <view wx:if="{{loading}}" class="mp-empty">加载中…</view>
  <view wx:elif="{{err}}" class="mp-err">{{err}}</view>
  <view wx:elif="{{!items.length}}" class="mp-empty">暂无投放计划</view>
  <scroll-view wx:else scroll-y class="mp-list-scroll">
    <view wx:for="{{items}}" wx:key="id" class="mp-list-card">
      <text class="mp-list-title">{{item.name}}</text>
      <text class="mp-list-tag">{{item.status}}</text>
      <text wx:if="{{item.budget}}" class="mp-list-meta">预算 {{item.budget}}</text>
    </view>
    <view class="mp-list-footer"></view>
  </scroll-view>
</view>


/* ===== FILE: 墨典商家小程序/pages/ads-manage/ads-manage.wxss ===== */


/* ===== FILE: 墨典商家小程序/pages/agent/agent.js ===== */
const api = require('../../utils/api.js')
const aiAgent = require('../../utils/aiAgentMp.js')
const registry = require('../../utils/aiModelRegistryMp.js')

const FILTER_TABS = [
  { id: 'all', short: '全部' },
  { id: 'chat', short: '对话' },
  { id: 'image', short: '生图' },
]

Page({
  data: {
    statusBarH: 44,
    headerH: 80,
    scrollBottomPad: 200,
    messages: [],
    input: '',
    busy: false,
    scrollTo: '',

墨典商家小程序软件 V1.0
第 13 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    attachments: [],
    attachmentFull: false,
    recording: false,
    inputMode: 'text',
    modelMenuOpen: false,
    modelFilter: 'all',
    filterTabs: FILTER_TABS,
    modelPickerKey: registry.loadPickerKey(),
    modelShort: '模型',
    filteredModelOptions: [],
  },

  onLoad() {
    this.recalcLayout()
    const allModelOptions = registry.listAiModelPickerOptions()
    this._allModelOptions = allModelOptions
    this._recorder = wx.getRecorderManager()
    this._recorder.onStop((res) => {
      this.setData({ recording: false })
      void this.onVoiceStop(res.tempFilePath)
    })
    this._recorder.onError(() => {
      this.setData({ recording: false })
      wx.showToast({ title: '录音失败', icon: 'none' })
    })

    const thread = aiAgent.loadThread()
    const messages =
      thread.length > 0
        ? thread
        : [
            {
              id: 'welcome',
              role: 'assistant',
              content: '你好，我是墨典 AI 智能体。输入文字或按住说话，右侧可切换模型。',
            },
          ]

    this.setData({ messages, modelPickerKey: registry.loadPickerKey() })
    this.syncModelPickers()
  },

  recalcLayout() {
    try {
      const sys = wx.getSystemInfoSync()
      const statusBarH = sys.statusBarHeight || 44
      const headerH = statusBarH + 48
      const tabBarPx = Math.round((76 * sys.windowWidth) / 750) + (sys.safeAreaInsets?.bottom || 0)
      const tabGapPx = Math.round((36 * sys.windowWidth) / 750)
      const dockPx = Math.round((120 * sys.windowWidth) / 750)

墨典商家小程序软件 V1.0
第 14 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      const attachPx = this.data.attachments.length
        ? Math.round((88 * sys.windowWidth) / 750)
        : 0
      this.setData({
        statusBarH,
        headerH,
        scrollBottomPad: headerH + dockPx + attachPx + tabBarPx + tabGapPx + 28,
      })
    } catch (_) {}
  },

  onShow() {
    if (!api.isAuthed()) {
      wx.redirectTo({ url: '/pages/login/login' })
      return
    }
    try {
      const app = getApp()
      if (app && typeof app.syncMerchantSession === 'function') void app.syncMerchantSession()
    } catch (_) {}
    if (typeof this.getTabBar === 'function' && this.getTabBar()) {
      this.getTabBar().setData({ selected: 0 })
    }
  },

  syncModelPickers() {
    const filtered = registry.filterOptions(this._allModelOptions, this.data.modelFilter)
    let pick = filtered.find((o) => o.key === this.data.modelPickerKey)
    if (!pick) pick = filtered[0]
    const key = pick ? pick.key : registry.defaultPickerKey()
    if (pick) registry.savePickerKey(key)
    const modelShort = pick ? registry.shortLabel(pick.label) : '模型'
    this.setData({
      filteredModelOptions: filtered,
      modelPickerKey: key,
      modelShort,
      attachmentFull: this.data.attachments.length >= aiAgent.MAX_ATTACH,
    })
    this.recalcLayout()
  },

  onInput(e) {
    this.setData({ input: e.detail.value })
  },

  onToggleInputMode() {
    const next = this.data.inputMode === 'text' ? 'voice' : 'text'
    this.setData({ inputMode: next, recording: false })
  },


墨典商家小程序软件 V1.0
第 15 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  onToggleModelMenu() {
    this.setData({ modelMenuOpen: !this.data.modelMenuOpen })
  },

  onCloseModelMenu() {
    this.setData({ modelMenuOpen: false })
  },

  onSheetCatch() {},

  onFilterTab(e) {
    const id = e.currentTarget.dataset.id
    this.setData({ modelFilter: id || 'all' })
    this.syncModelPickers()
  },

  onPickModel(e) {
    const key = e.currentTarget.dataset.key
    if (!key) return
    registry.savePickerKey(key)
    const pick = this._allModelOptions.find((o) => o.key === key)
    this.setData({
      modelPickerKey: key,
      modelShort: pick ? registry.shortLabel(pick.label) : '模型',
      modelMenuOpen: false,
    })
  },

  onNewChat() {
    aiAgent.clearThread()
    this.setData({
      messages: [
        {
          id: 'welcome',
          role: 'assistant',
          content: '已开始新对话。',
        },
      ],
      input: '',
      attachments: [],
      attachmentFull: false,
      modelMenuOpen: false,
    })
    this.recalcLayout()
  },

  onSend() {
    const text = (this.data.input || '').trim()
    if (!text && !this.data.attachments.length) return
    this.setData({ input: '' })

墨典商家小程序软件 V1.0
第 16 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    void this.sendTurn(text)
  },

  onAttach() {
    if (this.data.busy || this.data.attachmentFull) return
    const remain = aiAgent.MAX_ATTACH - this.data.attachments.length
    wx.showActionSheet({
      itemList: ['拍照', '从相册选图片', '从相册选视频'],
      success: (res) => {
        const tap = res.tapIndex
        if (tap === 0) this.pickMedia(['image'], ['camera'], remain)
        else if (tap === 1) this.pickMedia(['image'], ['album'], remain)
        else if (tap === 2) this.pickMedia(['video'], ['album'], remain)
      },
    })
  },

  pickMedia(mediaType, sourceType, count) {
    wx.chooseMedia({
      count,
      mediaType,
      sourceType,
      maxDuration: 60,
      success: async (res) => {
        const files = res.tempFiles || []
        const next = [...this.data.attachments]
        for (const f of files) {
          if (next.length >= aiAgent.MAX_ATTACH) break
          const isVideo = f.fileType === 'video' || /\.(mp4|mov|m4v)/i.test(f.tempFilePath || '')
          try {
            const previewPath = f.thumbTempFilePath || f.tempFilePath
            let dataUrl = ''
            if (isVideo) {
              if (!f.thumbTempFilePath) {
                wx.showToast({ title: '视频需首帧，请重选', icon: 'none' })
                continue
              }
              dataUrl = await aiAgent.readFileDataUrl(f.thumbTempFilePath, 'image/jpeg')
            } else {
              dataUrl = await aiAgent.readFileDataUrl(f.tempFilePath, 'image/jpeg')
            }
            next.push({
              id: `a-${Date.now()}-${next.length}`,
              kind: isVideo ? 'video' : 'image',
              preview: previewPath,
              dataUrl,
            })
          } catch (_) {
            wx.showToast({ title: '读取失败', icon: 'none' })
          }

墨典商家小程序软件 V1.0
第 17 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
        }
        this.setData({
          attachments: next,
          attachmentFull: next.length >= aiAgent.MAX_ATTACH,
        })
        this.recalcLayout()
      },
    })
  },

  onRemoveAttach(e) {
    const idx = Number(e.currentTarget.dataset.idx)
    const attachments = this.data.attachments.filter((_, i) => i !== idx)
    this.setData({
      attachments,
      attachmentFull: attachments.length >= aiAgent.MAX_ATTACH,
    })
    this.recalcLayout()
  },

  onPreviewImage(e) {
    const url = e.currentTarget.dataset.url
    if (!url) return
    const urls = []
    for (const m of this.data.messages) {
      if (m.imageUrls) urls.push(...m.imageUrls)
    }
    wx.previewImage({ current: url, urls: urls.length ? urls : [url] })
  },

  onVoiceStart() {
    if (this.data.busy || this.data.inputMode !== 'voice') return
    wx.authorize({
      scope: 'scope.record',
      success: () => {
        this.setData({ recording: true })
        this._recorder.start({ format: 'mp3', sampleRate: 16000 })
      },
      fail: () => {
        wx.showModal({
          title: '需要麦克风权限',
          content: '请在设置中允许录音。',
          showCancel: false,
        })
      },
    })
  },

  onVoiceEnd() {
    if (!this.data.recording) return

墨典商家小程序软件 V1.0
第 18 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    try {
      this._recorder.stop()
    } catch (_) {
      this.setData({ recording: false })
    }
  },

  async onVoiceStop(tempFilePath) {
    if (!tempFilePath) return
    wx.showLoading({ title: '识别中…' })
    const r = await aiAgent.transcribeVoiceTempPath(tempFilePath)
    wx.hideLoading()
    if (!r.ok) {
      wx.showToast({ title: r.message || '识别失败', icon: 'none' })
      return
    }
    if (this.data.inputMode === 'voice') {
      void this.sendTurn(r.text)
      return
    }
    const prev = (this.data.input || '').trim()
    this.setData({ input: prev ? `${prev} ${r.text}` : r.text })
  },

  async sendTurn(text) {
    if (this.data.busy) return
    const attachments = [...this.data.attachments]
    const line =
      String(text || '').trim() ||
      (attachments.length ? '请结合附图说明你的需求。' : '')
    if (!line && !attachments.length) return
    const pendingUser = {
      id: `u-${Date.now()}`,
      role: 'user',
      content: line,
      imageUrls: attachments.map((a) => a.preview).filter(Boolean),
    }
    const baseMessages = this.data.messages.concat([pendingUser])
    this.setData({
      busy: true,
      attachments: [],
      attachmentFull: false,
      messages: baseMessages,
      scrollTo: `msg-${pendingUser.id}`,
      modelMenuOpen: false,
    })
    this.recalcLayout()
    try {
      const turn = await aiAgent.sendAgentTurn({
        userLine: text,

墨典商家小程序软件 V1.0
第 19 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
        history: this.data.messages,
        attachments,
        pickerKey: this.data.modelPickerKey,
        modelOptions: this._allModelOptions,
      })
      const messages = baseMessages.concat([turn.assistantMsg])
      aiAgent.saveThread(messages)
      this.setData({
        messages,
        scrollTo: `msg-${turn.assistantMsg.id}`,
      })
    } catch (e) {
      const errMsg = {
        id: `e-${Date.now()}`,
        role: 'assistant',
        content: `暂时无法回复：${e.message || e}`,
      }
      this.setData({
        messages: baseMessages.concat([errMsg]),
        scrollTo: `msg-${errMsg.id}`,
      })
    } finally {
      this.setData({ busy: false })
    }
  },
})


/* ===== FILE: 墨典商家小程序/pages/agent/agent.json ===== */
{
  "navigationStyle": "custom",
  "usingComponents": {}
}


/* ===== FILE: 墨典商家小程序/pages/agent/agent.wxml ===== */
<view class="agent-page">
  <view class="agent-header" style="padding-top: {{statusBarH}}px;">
    <view class="header-row">
      <view class="menu-btn" bindtap="onNewChat">
        <text class="menu-glyph">≡</text>
      </view>
      <view class="header-center">
        <text class="header-title">墨典 AI 智能体</text>
      </view>
    </view>
  </view>

  <scroll-view
    scroll-y

墨典商家小程序软件 V1.0
第 20 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    class="chat-scroll"
    style="padding-top: {{headerH}}px; padding-bottom: {{scrollBottomPad}}px;"
    scroll-into-view="{{scrollTo}}"
    scroll-with-animation
    enhanced
    show-scrollbar="{{false}}"
  >
    <view class="chat-inner">
      <block wx:for="{{messages}}" wx:key="id">
        <view id="msg-{{item.id}}" class="msg-row {{item.role === 'user' ? 'user' : 'bot'}}">
          <view class="bubble {{item.role === 'user' ? 'bubble-user' : 'bubble-bot'}}">
            <view wx:if="{{item.imageUrls && item.imageUrls.length}}" class="msg-images">
              <image
                wx:for="{{item.imageUrls}}"
                wx:for-item="img"
                wx:key="*this"
                class="msg-thumb"
                src="{{img}}"
                mode="aspectFill"
                bindtap="onPreviewImage"
                data-url="{{img}}"
              />
            </view>
            <text class="bubble-text" user-select>{{item.content}}</text>
          </view>
        </view>
      </block>
      <view wx:if="{{busy}}" class="msg-row bot">
        <view class="bubble bubble-bot typing">
          <text class="bubble-text">思考中…</text>
        </view>
      </view>
    </view>
  </scroll-view>

  <view class="composer-dock">
    <view wx:if="{{attachments.length}}" class="attach-row">
      <view wx:for="{{attachments}}" wx:key="id" class="attach-item">
        <image class="attach-thumb" src="{{item.preview}}" mode="aspectFill" />
        <view class="attach-remove" data-idx="{{index}}" bindtap="onRemoveAttach">×</view>
      </view>
    </view>

    <view class="composer-bar">
      <view class="bar-plus {{attachmentFull ? 'disabled' : ''}}" bindtap="onAttach">
        <text class="bar-plus-txt">+</text>
      </view>

      <view class="bar-center">
        <block wx:if="{{inputMode === 'text'}}">

墨典商家小程序软件 V1.0
第 21 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
          <textarea
            class="bar-input"
            placeholder="发消息，可附图…"
            value="{{input}}"
            bindinput="onInput"
            auto-height
            maxlength="-1"
            show-confirm-bar="{{false}}"
            disabled="{{busy}}"
            confirm-type="send"
            bindconfirm="onSend"
          />
        </block>
        <view
          wx:else
          class="bar-hold {{recording ? 'recording' : ''}}"
          bindtouchstart="onVoiceStart"
          bindtouchend="onVoiceEnd"
          bindtouchcancel="onVoiceEnd"
        >
          <text>{{recording ? '松开发送' : '按住说话'}}</text>
        </view>
      </view>

      <view class="bar-mode" bindtap="onToggleInputMode">
        <image
          wx:if="{{inputMode === 'text'}}"
          class="bar-mode-img"
          src="/images/icon-mic.svg"
          mode="aspectFit"
        />
        <image
          wx:else
          class="bar-mode-img"
          src="/images/icon-keyboard.svg"
          mode="aspectFit"
        />
      </view>

      <view class="bar-model" bindtap="onToggleModelMenu">
        <text class="bar-model-txt">{{modelShort}}</text>
        <text class="bar-model-arrow">{{modelMenuOpen ? '▴' : '▾'}}</text>
      </view>
    </view>
  </view>

  <view wx:if="{{modelMenuOpen}}" class="model-mask" bindtap="onCloseModelMenu">
    <view class="model-sheet" catchtap="onSheetCatch">
      <view class="sheet-head">
        <text class="sheet-title">模型</text>

墨典商家小程序软件 V1.0
第 22 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
        <view class="sheet-tabs">
          <view
            wx:for="{{filterTabs}}"
            wx:key="id"
            class="sheet-tab {{modelFilter === item.id ? 'on' : ''}}"
            data-id="{{item.id}}"
            bindtap="onFilterTab"
          >{{item.short}}</view>
        </view>
      </view>
      <scroll-view scroll-y class="sheet-list" enhanced show-scrollbar="{{false}}">
        <view
          wx:for="{{filteredModelOptions}}"
          wx:key="key"
          class="sheet-item {{modelPickerKey === item.key ? 'on' : ''}}"
          data-key="{{item.key}}"
          bindtap="onPickModel"
        >
          <text>{{item.label}}</text>
        </view>
      </scroll-view>
    </view>
  </view>
</view>


/* ===== FILE: 墨典商家小程序/pages/agent/agent.wxss ===== */
.agent-page {
  height: 100vh;
  background: #f8fafc;
  overflow: hidden;
}

.agent-header {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  z-index: 100;
  background: #fff;
  border-bottom: 1rpx solid rgba(15, 23, 42, 0.06);
}

.header-row {
  display: flex;
  align-items: center;
  padding: 10rpx 20rpx 12rpx;
  gap: 12rpx;
}


墨典商家小程序软件 V1.0
第 23 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
.menu-btn {
  width: 60rpx;
  height: 60rpx;
  border-radius: 16rpx;
  background: #f1f5f9;
  display: flex;
  align-items: center;
  justify-content: center;
}

.menu-glyph {
  font-size: 30rpx;
  color: #334155;
  font-weight: 700;
}

.header-center {
  flex: 1;
}

.header-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #0f172a;
}

.chat-scroll {
  height: 100vh;
  box-sizing: border-box;
}

.chat-inner {
  padding: 12rpx 20rpx;
}

.msg-row {
  display: flex;
  margin-bottom: 14rpx;
}

.msg-row.user {
  justify-content: flex-end;
}

.msg-row.bot {
  justify-content: flex-start;
}

.bubble {
  max-width: 86%;

墨典商家小程序软件 V1.0
第 24 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  padding: 14rpx 18rpx;
  border-radius: 18rpx;
  line-height: 1.5;
}

.bubble-user {
  background: linear-gradient(135deg, #0ea5e9, #06b6d4);
  color: #fff;
}

.bubble-bot {
  background: #fff;
  color: #1e293b;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
}

.msg-images {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-bottom: 8rpx;
}

.msg-thumb {
  width: 112rpx;
  height: 112rpx;
  border-radius: 10rpx;
  background: #f1f5f9;
}

.bubble-text {
  font-size: 26rpx;
  white-space: pre-wrap;
  word-break: break-word;
}

.composer-dock {
  position: fixed;
  left: 16rpx;
  right: 16rpx;
  bottom: calc(76rpx + env(safe-area-inset-bottom) + 36rpx);
  z-index: 10001;
  padding-bottom: 4rpx;
}

.attach-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-bottom: 8rpx;

墨典商家小程序软件 V1.0
第 25 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  padding: 0 4rpx;
}

.attach-item {
  position: relative;
  width: 80rpx;
  height: 80rpx;
}

.attach-thumb {
  width: 80rpx;
  height: 80rpx;
  border-radius: 10rpx;
  background: #f1f5f9;
}

.attach-remove {
  position: absolute;
  top: -6rpx;
  right: -6rpx;
  width: 32rpx;
  height: 32rpx;
  border-radius: 999rpx;
  background: #0f172a;
  color: #fff;
  font-size: 22rpx;
  line-height: 32rpx;
  text-align: center;
}

.composer-bar {
  display: flex;
  align-items: center;
  gap: 10rpx;
  padding: 10rpx 12rpx;
  background: #fff;
  border-radius: 999rpx;
  border: 1rpx solid #e2e8f0;
  box-shadow: 0 6rpx 28rpx rgba(15, 23, 42, 0.12);
}

.bar-plus {
  width: 56rpx;
  height: 56rpx;
  border-radius: 999rpx;
  background: #f8fafc;
  border: 1rpx solid #e2e8f0;
  display: flex;
  align-items: center;
  justify-content: center;

墨典商家小程序软件 V1.0
第 26 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  flex-shrink: 0;
}

.bar-plus.disabled {
  opacity: 0.4;
}

.bar-plus-txt {
  font-size: 36rpx;
  color: #475569;
  line-height: 1;
  margin-top: -4rpx;
}

.bar-center {
  flex: 1;
  min-width: 0;
}

.bar-input {
  width: 100%;
  min-height: 56rpx;
  max-height: 160rpx;
  font-size: 28rpx;
  line-height: 1.4;
  padding: 8rpx 4rpx;
  box-sizing: border-box;
}

.bar-hold {
  height: 56rpx;
  line-height: 56rpx;
  text-align: center;
  font-size: 28rpx;
  color: #334155;
  font-weight: 500;
  border-radius: 12rpx;
}

.bar-hold.recording {
  background: #fef2f2;
  color: #dc2626;
}

.bar-mode {
  width: 56rpx;
  height: 56rpx;
  border-radius: 999rpx;
  background: #f8fafc;
  border: 1rpx solid #e2e8f0;

墨典商家小程序软件 V1.0
第 27 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.bar-mode-img {
  width: 36rpx;
  height: 36rpx;
  display: block;
}

.bar-model {
  max-width: 168rpx;
  height: 56rpx;
  padding: 0 14rpx;
  border-radius: 999rpx;
  background: #f0f9ff;
  border: 1rpx solid #bae6fd;
  display: flex;
  align-items: center;
  gap: 4rpx;
  flex-shrink: 0;
}

.bar-model-txt {
  font-size: 22rpx;
  color: #0369a1;
  font-weight: 600;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 120rpx;
}

.bar-model-arrow {
  font-size: 18rpx;
  color: #0284c7;
}

.model-mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 10002;
  background: rgba(15, 23, 42, 0.35);
}


墨典商家小程序软件 V1.0
第 28 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
.model-sheet {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  max-height: 62vh;
  background: #fff;
  border-radius: 24rpx 24rpx 0 0;
  padding-bottom: calc(76rpx + env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
}

.sheet-head {
  padding: 20rpx 24rpx 12rpx;
  border-bottom: 1rpx solid #f1f5f9;
}

.sheet-title {
  font-size: 22rpx;
  color: #94a3b8;
  display: block;
  margin-bottom: 12rpx;
}

.sheet-tabs {
  display: flex;
  gap: 10rpx;
}

.sheet-tab {
  padding: 8rpx 18rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  color: #64748b;
  background: #f8fafc;
}

.sheet-tab.on {
  background: #e0f2fe;
  color: #0369a1;
  font-weight: 600;
}

.sheet-list {
  flex: 1;
  height: 0;
  max-height: 48vh;
}


墨典商家小程序软件 V1.0
第 29 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
.sheet-item {
  padding: 22rpx 28rpx;
  font-size: 26rpx;
  color: #334155;
  border-bottom: 1rpx solid #f8fafc;
}

.sheet-item.on {
  color: #0369a1;
  font-weight: 600;
  background: #f0f9ff;
}


/* ===== FILE: 墨典商家小程序/pages/ai-content/ai-content.js ===== */
const api = require('../../utils/api.js')
const feature = require('../../utils/merchantFeatureMp.js')

Page({
  data: { tab: 'article', brief: '', out: '', err: '', busy: false },
  onShow() {
    if (!api.getAccessToken()) wx.redirectTo({ url: '/pages/login/login' })
  },
  onTab(e) {
    this.setData({ tab: e.currentTarget.dataset.t, out: '', err: '' })
  },
  onBrief(e) {
    this.setData({ brief: e.detail.value })
  },
  async onGenerate() {
    const brief = (this.data.brief || '').trim()
    if (brief.length < 4) {
      wx.showToast({ title: '请先填写需求', icon: 'none' })
      return
    }
    const scene = this.data.tab === 'topic' ? 'operation_topic' : 'operation_article'
    this.setData({ busy: true, err: '', out: '' })
    const r = await feature.postAiAssist(scene, brief)
    this.setData({ busy: false })
    if (!r.ok) {
      this.setData({ err: r.message })
      return
    }
    this.setData({ out: r.text })
  },
  onCopy() {
    if (!this.data.out) return
    wx.setClipboardData({
      data: this.data.out,
      success() {

墨典商家小程序软件 V1.0
第 30 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
        wx.showToast({ title: '已复制' })
      },
    })
  },
})


/* ===== FILE: 墨典商家小程序/pages/ai-content/ai-content.json ===== */
{ "navigationBarTitleText": "AI 文章话题" }


/* ===== FILE: 墨典商家小程序/pages/ai-content/ai-content.wxml ===== */
<scroll-view scroll-y class="mp-page" style="height:100vh">
  <view class="mp-tabs">
    <view class="mp-tab {{tab === 'article' ? 'active' : ''}}" data-t="article" bindtap="onTab">文章</view>
    <view class="mp-tab {{tab === 'topic' ? 'active' : ''}}" data-t="topic" bindtap="onTab">话题</view>
  </view>
  <view class="mp-form-card">
    <textarea class="meoo-input area" value="{{brief}}" bindinput="onBrief" placeholder="{{tab === 'article' ? '描述要写的探店/种草文章' : '描述账号定位与想蹭的热点'}}" />
    <button class="meoo-btn-primary" bindtap="onGenerate" loading="{{busy}}">AI 生成</button>
    <view wx:if="{{err}}" class="mp-err" style="text-align:left;margin-top:16rpx">{{err}}</view>
    <view wx:if="{{out}}" class="mp-output">{{out}}</view>
    <button wx:if="{{out}}" class="meoo-btn-ghost" style="margin-top:20rpx" bindtap="onCopy">复制全文</button>
  </view>
  <view class="mp-list-footer"></view>
</scroll-view>


/* ===== FILE: 墨典商家小程序/pages/ai-content/ai-content.wxss ===== */
.area { min-height: 200rpx; }


/* ===== FILE: 墨典商家小程序/pages/dashboard/dashboard.js ===== */
const api = require('../../utils/api.js')
const merchant = require('../../utils/merchantApi.js')
const dashboardMp = require('../../utils/dashboardMp.js')
const { PLATFORM_TABS, readPlatformToken } = require('../../utils/platformTokensMp.js')

const TONE_BY_PLAT = {
  douyin: 'pink',
  meituan: 'amber',
  xiaohongshu: 'rose',
  jd: 'red',
}

/** 经营概览：与电脑端首页类似的汇总指标 */
Page({
  data: {
    loading: false,
    rangeLabel: '近 7 日',
