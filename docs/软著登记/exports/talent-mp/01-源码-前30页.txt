灵祺星选达人撮合小程序软件 V1.0
第 1 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
/* ===== FILE: 灵祺达人撮合小程序/app.js ===== */
const chatBadgeWatcher = require('./utils/chatBadgeWatcher.js')
const auth = require('./utils/auth.js')
const config = require('./utils/config.js')
const mpShare = require('./utils/mpShare.js')

App({
  globalData: {
    chatBadge: 0,
  },
  onLaunch() {
    mpShare.enableShareMenu()
    mpShare.preloadShareCover()
    if (config.MP_USE_CLOUD_PROXY && wx.cloud) {
      const env = String(config.MP_CLOUD_ENV || '').trim()
      if (env) {
        wx.cloud.init({ env, traceUser: true })
      } else {
        console.warn('[mp] MP_USE_CLOUD_PROXY 已开但 MP_CLOUD_ENV 为空，见 备案过渡-云开发代理.md')
      }
    }
    chatBadgeWatcher.start()
    if (auth.isLoggedIn()) {
      try {
        require('./utils/mpAccountClientSync.js').pullAfterLogin()
      } catch (_) {}
    }
  },
  onShow() {
    void chatBadgeWatcher.refreshNow()
    if (auth.isLoggedIn()) {
      auth
        .refreshSession()
        .then(() => {
          try {
            return require('./utils/registryProfileSync.js').pullRegistryProfileAfterLogin()
          } catch (_) {
            return null
          }
        })
        .catch(() => {})
    }
  },
})


/* ===== FILE: 灵祺达人撮合小程序/app.json ===== */
{
  "pages": [
    "pages/welcome/welcome",

灵祺星选达人撮合小程序软件 V1.0
第 2 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    "pages/index/index",
    "pages/login/login",
    "pages/recommend/recommend",
    "pages/publish/publish",
    "pages/messages/messages",
    "pages/mine/mine",
    "pages/register/register",
    "pages/detail/detail",
    "pages/apply/apply",
    "pages/mine-applications/mine-applications",
    "pages/mine-templates/mine-templates",
    "pages/mine-template-edit/mine-template-edit",
    "pages/mine-notifications/mine-notifications",
    "pages/mine-notification-detail/mine-notification-detail",
    "pages/mine-analytics/mine-analytics",
    "pages/mine-support/mine-support",
    "pages/mine-support-chat/mine-support-chat",
    "pages/mine-pr-profile/mine-pr-profile",
    "pages/mine-pr-orders/mine-pr-orders",
    "pages/mine-pr-order-applicants/mine-pr-order-applicants",
    "pages/mine-form-relay/mine-form-relay",
    "pages/mine-pr-order-video-review/mine-pr-order-video-review",
    "pages/chat/chat",
    "pages/legal/legal",
    "pages/web-link/web-link"
  ],
  "window": {
    "navigationBarTitleText": "灵祺星选平台",
    "navigationBarBackgroundColor": "#9b87f5",
    "navigationBarTextStyle": "white",
    "backgroundColor": "#f0eff6",
    "backgroundTextStyle": "dark"
  },
  "tabBar": {
    "custom": true,
    "color": "#94a3b8",
    "selectedColor": "#7c83ff",
    "backgroundColor": "#ffffff",
    "list": [
      { "pagePath": "pages/index/index", "text": "首页" },
      { "pagePath": "pages/recommend/recommend", "text": "推荐" },
      { "pagePath": "pages/publish/publish", "text": "发招募" },
      { "pagePath": "pages/messages/messages", "text": "消息" },
      { "pagePath": "pages/mine/mine", "text": "我的" }
    ]
  },
  "sitemapLocation": "sitemap.json",
  "lazyCodeLoading": "requiredComponents",
  "networkTimeout": {
    "request": 120000,

灵祺星选达人撮合小程序软件 V1.0
第 3 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    "downloadFile": 120000,
    "connectSocket": 60000
  },
  "navigateToMiniProgramAppIdList": [
    "wx8b6c33d344f46d19"
  ]
}


/* ===== FILE: 灵祺达人撮合小程序/app.wxss ===== */
@import './styles/theme.wxss';
@import './styles/subpage-nav.wxss';

page {
  background: var(--lq-bg);
  color: var(--lq-text);
  font-size: 28rpx;
  line-height: 1.55;
  box-sizing: border-box;
}

.page-shell {
  min-height: 100vh;
}

.page-with-bar {
  padding-bottom: calc(100rpx + env(safe-area-inset-bottom));
}

/* Tab 页：顶栏/筛选固定，仅中间列表区滚动 */
.page-shell-fixed {
  display: flex;
  flex-direction: column;
  height: 100vh;
  overflow: hidden;
  box-sizing: border-box;
}

.wx-identity-id {
  display: block;
  margin-top: 8rpx;
  font-size: 22rpx;
  font-weight: 700;
  color: var(--lq-primary-dark);
  letter-spacing: 0.5rpx;
}

.page-scroll-body {
  flex: 1;
  height: 0;

灵祺星选达人撮合小程序软件 V1.0
第 4 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  width: 100%;
  box-sizing: border-box;
}

/* PR 身份中间「发招募」凸起按钮 */
.page-with-bar--fab {
  padding-bottom: calc(132rpx + env(safe-area-inset-bottom));
}

.page-hero {
  margin: 0 28rpx 8rpx;
  padding: 36rpx 32rpx 32rpx;
  border-radius: var(--lq-radius-lg);
  background: var(--lq-gradient-deep);
  box-shadow: var(--lq-shadow-header);
}

.page-hero-title {
  display: block;
  font-size: 40rpx;
  font-weight: 700;
  color: #fff;
}

.page-hero-sub {
  display: block;
  margin-top: 12rpx;
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.88);
}

.card {
  margin: 20rpx 24rpx 0;
  padding: 32rpx;
  background: var(--lq-card);
  border-radius: var(--lq-radius-md);
  border: 3rpx solid var(--lq-border);
  box-shadow: var(--lq-shadow-card);
}

.card--accent {
  background: linear-gradient(145deg, #ffffff 0%, #fdf4ff 100%);
  border-color: #ede9fe;
  box-shadow: 0 10rpx 0 rgba(237, 233, 254, 0.85);
}

.hint {
  color: var(--lq-text-secondary);
  font-size: 24rpx;
  line-height: 1.55;

灵祺星选达人撮合小程序软件 V1.0
第 5 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

.title-lg {
  font-size: 34rpx;
  font-weight: 700;
  color: var(--lq-text);
}

.section-head {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.section-head-dot {
  width: 8rpx;
  height: 32rpx;
  border-radius: 4rpx;
  background: var(--lq-gradient);
}

.section-title {
  font-size: 30rpx;
  font-weight: 700;
  flex: 1;
}

.section-title--plain {
  margin-bottom: 20rpx;
}

.tag {
  display: inline-block;
  margin-right: 8rpx;
  margin-top: 8rpx;
  padding: 6rpx 16rpx;
  border-radius: 8rpx;
  font-size: 22rpx;
  font-weight: 500;
}

.tag-pink {
  background: #fceef4;
  color: #b84d7a;
}

.tag-blue {
  background: var(--lq-primary-50);
  color: var(--lq-primary-dark);

灵祺星选达人撮合小程序软件 V1.0
第 6 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

.tag-gray {
  background: #f3f2f7;
  color: #6b6e82;
}

.tag-green {
  background: #fef6e8;
  color: #b8863a;
}

.tag-urgent {
  background: #fff0f2;
  color: #d45d6e;
}

.tag-ice {
  background: #f0f1fc;
  color: #6b72c4;
}

.btn-primary {
  background: var(--lq-gradient);
  color: #fff;
  border-radius: 999rpx;
  font-size: 30rpx;
  font-weight: 600;
  border: 3rpx solid rgba(255, 255, 255, 0.45);
  box-shadow: 0 10rpx 0 rgba(183, 148, 246, 0.45);
}

.btn-primary::after {
  border: none;
}

.btn-secondary {
  background: #f8fafc;
  color: #475569;
  border-radius: 12rpx;
  font-size: 28rpx;
  border: none;
}

.btn-secondary::after {
  border: none;
}

.btn-ghost {
  background: #fff;

灵祺星选达人撮合小程序软件 V1.0
第 7 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  color: var(--lq-primary-dark);
  border: 2rpx solid var(--lq-primary-100);
  border-radius: 999rpx;
}

.btn-ghost::after {
  border: none;
}

.btn-wx {
  background: var(--lq-gradient);
  color: #fff;
  border-radius: 999rpx;
  border: none;
}

.btn-wx::after {
  border: none;
}

.link-text {
  color: var(--lq-primary-dark);
  font-weight: 500;
}

.input,
.picker-val {
  padding: 22rpx 24rpx;
  min-height: 88rpx;
  line-height: 44rpx;
  background: #fdfbff;
  border: 3rpx solid var(--lq-border);
  border-radius: var(--lq-radius-sm);
  font-size: 28rpx;
  box-sizing: border-box;
}

.input {
  width: 100%;
  height: auto;
}

picker {
  display: block;
  width: 100%;
}

.picker-val {
  display: flex;
  align-items: center;

灵祺星选达人撮合小程序软件 V1.0
第 8 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  width: 100%;
  word-break: break-all;
}

.picker-val--placeholder {
  color: #94a3b8;
}

.apply-form .form-row {
  margin-top: 24rpx;
}

.apply-form .form-label {
  display: block;
  font-size: 24rpx;
  font-weight: 500;
  color: #475569;
  margin-bottom: 10rpx;
}

.form-row-inline {
  display: flex;
  gap: 20rpx;
}

.form-col {
  flex: 1;
}

.state-loading,
.state-error,
.state-empty {
  text-align: center;
  padding: 48rpx 24rpx;
}

.state-error {
  color: #dc2626;
}

.state-empty-icon {
  display: block;
  font-size: 64rpx;
  margin-bottom: 16rpx;
  opacity: 0.45;
}

.loading-dot-wrap {
  display: flex;
  justify-content: center;

灵祺星选达人撮合小程序软件 V1.0
第 9 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  gap: 12rpx;
}

.loading-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--lq-primary);
  animation: lq-pulse 1.2s ease-in-out infinite;
}

.loading-dot:nth-child(2) {
  animation-delay: 0.15s;
}

.loading-dot:nth-child(3) {
  animation-delay: 0.3s;
}

@keyframes lq-pulse {
  0%,
  100% {
    opacity: 0.35;
    transform: scale(0.85);
  }
  50% {
    opacity: 1;
    transform: scale(1);
  }
}

.metrics {
  display: flex;
  border-radius: var(--lq-radius-sm);
  overflow: hidden;
  background: var(--lq-primary-50);
  border: 1rpx solid var(--lq-border);
}

.metric-cell {
  flex: 1;
  padding: 22rpx 12rpx;
  text-align: center;
  border-right: 1rpx solid var(--lq-border);
}

.metric-cell:last-child {
  border-right: none;
}


灵祺星选达人撮合小程序软件 V1.0
第 10 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
.metric-label {
  font-size: 22rpx;
  color: var(--lq-text-secondary);
}

.metric-value {
  margin-top: 8rpx;
  font-size: 28rpx;
  font-weight: 700;
}

.metric-value.accent {
  color: var(--lq-primary-dark);
}

.merchant-name {
  font-size: 26rpx;
  color: var(--lq-text-secondary);
}

.bottom-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 10rpx 20rpx calc(10rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(12px);
  border-top: 1rpx solid var(--lq-border);
  box-shadow: 0 -6rpx 24rpx rgba(90, 96, 140, 0.06);
}

.bottom-bar__tools {
  display: flex;
  flex-shrink: 0;
  align-items: center;
  gap: 4rpx;
}

.bottom-bar__actions {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 0;
}

灵祺星选达人撮合小程序软件 V1.0
第 11 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————

.bottom-icon {
  margin: 0;
  padding: 0;
  width: 68rpx !important;
  min-width: 68rpx;
  height: 64rpx;
  line-height: 64rpx;
  font-size: 22rpx;
  font-weight: 500;
  color: #64748b;
  background: transparent;
  border: none;
  border-radius: 12rpx;
}

.bottom-icon::after {
  border: none;
}

.bottom-bar .bottom-cta {
  flex: 1;
  min-width: 0;
  margin: 0;
  padding: 0 20rpx;
  height: 72rpx;
  line-height: 72rpx;
  font-size: 26rpx;
  font-weight: 600;
  border-radius: 36rpx;
  box-sizing: border-box;
}

.bottom-bar .btn-ghost.bottom-cta {
  line-height: 68rpx;
  font-size: 24rpx;
  padding: 0 16rpx;
}

.bottom-bar .btn-secondary.bottom-cta--muted {
  flex: 1;
  min-width: 0;
  color: var(--lq-text-secondary);
  background: #f1f5f9;
  border: 2rpx solid var(--lq-border);
}

.bottom-bar .btn-primary.bottom-cta {
  font-size: 26rpx;
  box-shadow: 0 6rpx 18rpx rgba(114, 121, 217, 0.18);

灵祺星选达人撮合小程序软件 V1.0
第 12 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

.line-item {
  padding: 16rpx 0;
  font-size: 26rpx;
  border-bottom: 1rpx solid #f1f5f9;
}

.steps .step-item {
  display: flex;
  gap: 20rpx;
  padding: 20rpx 0;
}

.step-num {
  width: 44rpx;
  height: 44rpx;
  line-height: 44rpx;
  text-align: center;
  border-radius: 50%;
  background: var(--lq-gradient);
  color: #fff;
  font-size: 24rpx;
  font-weight: 700;
}

/* 招募大厅 / 推荐商单 · 价格多选弹层 */
.price-sheet-mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 10000;
  background: rgba(53, 50, 74, 0.32);
  display: flex;
  align-items: flex-end;
}

.price-sheet {
  width: 100%;
  padding: 28rpx 28rpx calc(28rpx + env(safe-area-inset-bottom));
  background: #fff;
  border-radius: 32rpx 32rpx 0 0;
  box-shadow: 0 -12rpx 48rpx rgba(15, 23, 42, 0.12);
}

.price-sheet-head {
  display: flex;
  align-items: center;

灵祺星选达人撮合小程序软件 V1.0
第 13 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.price-sheet-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #0f172a;
}

.price-sheet-close {
  font-size: 44rpx;
  color: #94a3b8;
  line-height: 1;
  padding: 0 8rpx;
}

.price-chip-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.price-chip {
  padding: 14rpx 28rpx;
  border-radius: 999rpx;
  font-size: 26rpx;
  color: #475569;
  background: #f1f5f9;
  border: 2rpx solid #e2e8f0;
}

.price-chip--on {
  color: var(--lq-primary-dark);
  background: var(--lq-primary-50);
  border-color: var(--lq-primary-soft);
  font-weight: 600;
}

.price-sheet-foot {
  display: flex;
  gap: 20rpx;
  margin-top: 28rpx;
}

.price-sheet-reset,
.price-sheet-ok {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;

灵祺星选达人撮合小程序软件 V1.0
第 14 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  border-radius: 999rpx;
  font-size: 28rpx;
  border: none;
}

.price-sheet-reset {
  background: #f1f5f9;
  color: #64748b;
}

.price-sheet-reset::after,
.price-sheet-ok::after {
  border: none;
}

.price-sheet-ok {
  background: var(--lq-gradient);
  color: #fff;
  font-weight: 600;
}


/* ===== FILE: 灵祺达人撮合小程序/behaviors/mpDefaultShare.js ===== */
/** 未自定义分享的页面：统一 AI 封面 + 跳转招募大厅 */
const mpShare = require('../utils/mpShare.js')

module.exports = Behavior({
  pageLifetimes: {
    show() {
      mpShare.enableShareMenu()
    },
  },
  methods: {
    onShareAppMessage() {
      return mpShare.defaultShare('/pages/index/index')
    },
    onShareTimeline() {
      return mpShare.defaultTimelineShare()
    },
  },
})


/* ===== FILE: 灵祺达人撮合小程序/cloudfunctions/mpErpProxy/config.json ===== */
{
  "permissions": {
    "openapi": []
  },
  "timeout": 60,
  "memorySize": 256

灵祺星选达人撮合小程序软件 V1.0
第 15 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}


/* ===== FILE: 灵祺达人撮合小程序/cloudfunctions/mpErpProxy/erp-target.js ===== */
/**
 * 备案期默认连阿里云轻量（改 IP 后重新「上传并部署」云函数即可，无需控制台环境变量）
 */
/** 阿里云控制台 → 轻量应用服务器 → 复制「公网 IP」，改完重新部署云函数 */
module.exports = {
  ip: '139.196.42.5',
  host: 'mofangdianai.com',
  tlsSni: 'mofangdianai.com',
  useIpHost: true,
  /** 腾讯云 → 轻量：443 常 reset，优先 80 + Host=IP（须 Nginx 80-ip snippet） */
  http80IpOnly: true,
  https: true,
  httpsIpOnly: true,
  altPort: 0,
  insecure: false,
}


/* ===== FILE: 灵祺达人撮合小程序/cloudfunctions/mpErpProxy/index.js ===== */
/**
 * 备案期：手机 → 云函数 → 轻量 IP（读 erp-target.js）
 */
const cloud = require('wx-server-sdk')
const https = require('https')
const http = require('http')
const target = require('./erp-target.js')

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

const PROXY_BUILD = 'mpErpProxy-20260603-post-once'
const ORIGIN = String(process.env.ECS_ERP_ORIGIN || '').replace(/\/$/, '')
const ERP_IP = String(process.env.ECS_ERP_IP || target.ip || '').trim()
const ERP_HOST = String(process.env.ECS_ERP_HOST || target.host || 'mofangdianai.com').trim()
const TLS_SNI = String(target.tlsSni || ERP_HOST).trim()

function requestHost(useIp) {
  if (useIp && target.useIpHost !== false && ERP_IP) return ERP_IP
  return ERP_HOST
}

function apiPath(raw) {
  let p = String(raw || '').trim()
  if (!p.startsWith('/')) p = `/${p}`
  if (p.startsWith('/api/')) p = `/erp-api${p.slice(4)}`
  else if (!p.startsWith('/erp-api')) p = `/erp-api${p}`
  return p

灵祺星选达人撮合小程序软件 V1.0
第 16 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

function ipTarget(path, { https, port, useIp }) {
  const useHttps = https === true
  const p = port || (useHttps ? 443 : 80)
  const host = requestHost(useIp)
  return {
    mode: 'ip',
    proto: useHttps ? 'https' : 'http',
    hostname: ERP_IP,
    port: p,
    path,
    host,
    sni: TLS_SNI,
  }
}

function urlTarget(path) {
  const base = ORIGIN || `https://${ERP_HOST}/erp-api`
  const rel = path.replace(/^\/erp-api/, '') || '/'
  return { mode: 'url', fullUrl: `${base.replace(/\/$/, '')}${rel}` }
}

function buildAttempts(path) {
  if (ORIGIN) return [urlTarget(path)]
  if (!ERP_IP) return [urlTarget(path)]
  const list = []
  if (target.http80IpOnly) {
    list.push(ipTarget(path, { https: false, port: 80, useIp: true }))
  }
  if (target.https !== false) {
    list.push(ipTarget(path, { https: true, useIp: true }))
    if (!target.httpsIpOnly) list.push(ipTarget(path, { https: true, useIp: false }))
  }
  const alt = Number(target.altPort)
  if (alt > 0) list.push(ipTarget(path, { https: false, port: alt, useIp: true }))
  return list
}

function upstreamRequest(t, method, body, headers) {
  return new Promise((resolve, reject) => {
    const payload = body != null && method !== 'GET' ? JSON.stringify(body) : ''
    const reqPath = t.mode === 'ip' ? t.path : `${new URL(t.fullUrl).pathname}${new URL(t.fullUrl).search}`
    const timeoutMs = /video-upload/i.test(reqPath)
      ? 120000
      : /registry|hall-registry|publisher-display|meoo-ops-mp-auth/i.test(reqPath)
        ? 45000
        : 20000
    let opts
    if (t.mode === 'ip') {

灵祺星选达人撮合小程序软件 V1.0
第 17 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      opts = {
        hostname: t.hostname,
        port: t.port,
        path: t.path,
        method,
        headers: {
          Accept: 'application/json',
          Host: t.host,
          ...(method !== 'GET'
            ? { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(payload) }
            : {}),
          ...headers,
        },
        servername: t.sni || t.host,
        rejectUnauthorized: target.insecure !== true,
        timeout: timeoutMs,
      }
    } else {
      const u = new URL(t.fullUrl)
      opts = {
        hostname: u.hostname,
        port: u.port || (u.protocol === 'https:' ? 443 : 80),
        path: `${u.pathname}${u.search}`,
        method,
        headers: {
          Accept: 'application/json',
          Host: u.host,
          ...(method !== 'GET'
            ? { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(payload) }
            : {}),
          ...headers,
        },
        servername: u.hostname,
        rejectUnauthorized: true,
        timeout: timeoutMs,
      }
    }
    const lib =
      t.mode === 'ip' ? (t.proto === 'https' ? https : http) : t.fullUrl.startsWith('https') ? https : http
    const req = lib.request(opts, (res) => {
      const chunks = []
      res.on('data', (c) => chunks.push(c))
      res.on('end', () => {
        const raw = Buffer.concat(chunks).toString('utf8')
        let data
        try {
          data = raw ? JSON.parse(raw) : {}
        } catch {
          data = { message: raw.slice(0, 500) }
        }

灵祺星选达人撮合小程序软件 V1.0
第 18 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
        resolve({ status: res.statusCode || 0, data })
      })
    })
    req.on('error', reject)
    req.setTimeout(timeoutMs, () => {
      req.destroy(new Error('timeout'))
    })
    if (payload) req.write(payload)
    req.end()
  })
}

function describeTarget(t) {
  if (t.mode === 'ip') return `${t.proto}://Host:${t.host}${t.path}@${t.hostname}:${t.port}(sni:${t.sni})`
  return t.fullUrl
}

function isIcpBlock(data) {
  const s = JSON.stringify(data || '')
  return /ICP Filing|beian-block|Non-compliance/i.test(s)
}

exports.main = async (event) => {
  const method = String(event.method || 'GET').toUpperCase()
  const path = apiPath(event.path || '/api/mp-cronet-ping')
  const body = event.body
  const headers = event.headers && typeof event.headers === 'object' ? event.headers : {}
  const attempts = buildAttempts(path)
  const trace = []
  let lastFail = null

  for (const t of attempts) {
    const label = describeTarget(t)
    try {
      const { status, data } = await upstreamRequest(t, method, body, headers)
      if (status >= 200 && status < 300) {
        return {
          ok: true,
          status,
          data,
          via: 'cloud-mpErpProxy',
          upstream: label,
          build: PROXY_BUILD,
        }
      }
      const apiErr = data && typeof data.error === 'string' ? data.error : ''
      const note = isIcpBlock(data) ? 'icp-block' : apiErr || `http-${status}`
      trace.push(`${label} → ${note}`)
      lastFail = { status, data, upstream: label }
      /** 微信 login code 一次性，禁止换端口重试以免 code 作废 */

灵祺星选达人撮合小程序软件 V1.0
第 19 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      if (method === 'POST') {
        break
      }
    } catch (e) {
      trace.push(`${label} → ${e.message || e}`)
      if (method === 'POST') break
    }
  }

  return {
    ok: false,
    status: lastFail ? lastFail.status : 0,
    data: lastFail ? lastFail.data : undefined,
    error: trace.join(' | ') || 'all attempts failed',
    via: 'cloud-mpErpProxy',
    build: PROXY_BUILD,
    upstream: lastFail ? lastFail.upstream : undefined,
    hint:
      '登录 http-500：在 ECS 执行 curl POST meoo-ops-mp-auth 与 journalctl -u meoo-auth-api；常见为微信密钥或 mp_accounts 表',
  }
}


/* ===== FILE: 灵祺达人撮合小程序/cloudfunctions/mpErpProxy/package.json ===== */
{
  "name": "mpErpProxy",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "~2.6.3"
  }
}


/* ===== FILE: 灵祺达人撮合小程序/components/login-orbit-wall/index.js ===== */
/** 展开大图与环墙同图，避免主包打入 -hd 冗余资源（微信主包 2MB 限制） */
function hdSrcFor(src) {
  return String(src || '')
}

function buildFaces(images) {
  const list = Array.isArray(images) ? images.filter(Boolean) : []
  const n = list.length
  if (!n) return []
  const step = 360 / n
  return list.map((src, i) => ({
    src,
    hdSrc: hdSrcFor(src),
    deg: Math.round(step * i * 10) / 10,
  }))

灵祺星选达人撮合小程序软件 V1.0
第 20 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

function yawTransform(yaw) {
  const y = Math.round(Number(yaw) * 10) / 10
  return `rotateY(${y}deg)`
}

Component({
  properties: {
    images: {
      type: Array,
      value: [],
    },
    hint: {
      type: String,
      value: '左右滑动旋转达人样片 · 点击查看大图',
    },
  },

  data: {
    yaw: 0,
    ringTransform: 'rotateY(0deg)',
    radiusPx: 120,
    faces: [],
    ringSnap: false,
    expanded: false,
    expandedIndex: 0,
    faceCount: 0,
  },

  observers: {
    images(imgs) {
      const faces = buildFaces(imgs)
      this.setData({
        faces,
        faceCount: faces.length,
      })
    },
  },

  lifetimes: {
    attached() {
      try {
        const win = wx.getWindowInfo ? wx.getWindowInfo() : wx.getSystemInfoSync()
        const rpx = win.windowWidth / 750
        const faces = buildFaces(this.properties.images)
        this.setData({
          radiusPx: Math.round(340 * rpx),
          faces,
          faceCount: faces.length,

灵祺星选达人撮合小程序软件 V1.0
第 21 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
          ringTransform: yawTransform(0),
        })
      } catch (_) {
        const faces = buildFaces(this.properties.images)
        this.setData({ faces, faceCount: faces.length, ringTransform: yawTransform(0) })
      }
      this._snapTimer = null
    },
    detached() {
      if (this._snapTimer) clearTimeout(this._snapTimer)
    },
  },

  methods: {
    /** WXS 拖动开始：关闭 CSS 过渡 */
    orbitDragStart() {
      if (this._snapTimer) {
        clearTimeout(this._snapTimer)
        this._snapTimer = null
      }
      this.setData({ ringSnap: false })
    },

    /** WXS 拖动结束：吸附到正面卡位并写回 data（与 WXS state 一致） */
    orbitDragEnd(e) {
      const detail = e || {}
      const n = this.data.faceCount || this.data.faces.length || 6
      const step = 360 / n
      let yaw = Number(detail.yaw) || 0
      yaw = ((yaw % 360) + 360) % 360
      yaw = Math.round(yaw / step) * step
      const ringTransform = yawTransform(yaw)
      if (this._snapTimer) clearTimeout(this._snapTimer)
      this.setData({
        yaw,
        ringTransform,
        ringSnap: true,
      })
      this._snapTimer = setTimeout(() => {
        this.setData({ ringSnap: false })
        this._snapTimer = null
      }, 320)
    },

    orbitOpenExpand(e) {
      const index = Number(e && e.index)
      if (Number.isFinite(index)) this._openExpand(index)
    },

    _openExpand(index) {

灵祺星选达人撮合小程序软件 V1.0
第 22 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      const n = this.data.faceCount || this.data.faces.length
      if (!n) return
      const i = Math.max(0, Math.min(index, n - 1))
      this.setData({
        expanded: true,
        expandedIndex: i,
        ringSnap: false,
      })
      this.triggerEvent('expand', { index: i })
    },

    onOpenExpand(e) {
      const index = Number(e.currentTarget.dataset.index)
      if (!Number.isFinite(index)) return
      this._openExpand(index)
    },

    onCloseExpand() {
      this.setData({ expanded: false })
      this.triggerEvent('collapse')
    },

    onSwiperChange(e) {
      const idx = e.detail && e.detail.current
      if (typeof idx === 'number') {
        this.setData({ expandedIndex: idx })
      }
    },

    onImgError(e) {
      const idx = e && e.currentTarget && e.currentTarget.dataset
      const slideIndex = idx && idx.slideIndex != null ? Number(idx.slideIndex) : -1
      const faces = this.data.faces || []
      if (slideIndex >= 0 && faces[slideIndex] && faces[slideIndex].hdSrc !== faces[slideIndex].src) {
        const next = faces.map((f, i) =>
          i === slideIndex ? { ...f, hdSrc: f.src } : f,
        )
        this.setData({ faces: next })
        return
      }
      wx.showToast({ title: '图片加载失败', icon: 'none' })
    },
  },
})


/* ===== FILE: 灵祺达人撮合小程序/components/login-orbit-wall/index.json ===== */
{
  "component": true,
  "usingComponents": {}

灵祺星选达人撮合小程序软件 V1.0
第 23 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}


/* ===== FILE: 灵祺达人撮合小程序/components/login-orbit-wall/index.wxml ===== */
<wxs module="ot" src="./orbit-touch.wxs" />

<view class="orbit">
  <view class="orbit__hint">
    <text class="orbit__hint-txt" wx:if="{{!expanded}}">{{hint}}</text>
    <text class="orbit__hint-txt" wx:else>左右滑动切换 · 点击收起（{{expandedIndex + 1}}/{{faceCount}}）</text>
  </view>

  <view class="orbit__stage {{expanded ? 'orbit__stage--expanded' : ''}}">
    <block wx:if="{{!expanded}}">
      <view
        class="orbit__ring-wrap"
        data-yaw="{{yaw}}"
        bindtouchstart="{{ot.start}}"
        bindtouchmove="{{ot.move}}"
        bindtouchend="{{ot.end}}"
        bindtouchcancel="{{ot.end}}"
      >
        <view
          class="orbit__ring {{ringSnap ? 'orbit__ring--snap' : ''}}"
          style="transform: {{ringTransform}};"
        >
          <view
            wx:for="{{faces}}"
            wx:key="src"
            wx:for-index="faceIndex"
            class="orbit__face"
            style="transform: rotateY({{item.deg}}deg) translateZ({{radiusPx}}px);"
          >
            <view class="orbit__card" catchtap="onOpenExpand" data-index="{{faceIndex}}">
              <image class="orbit__img" src="{{item.src}}" mode="aspectFill" />
              <view class="orbit__shine" aria-hidden="true" />
              <view class="orbit__zoom-hint" aria-hidden="true">
                <text class="orbit__zoom-icon">⊕</text>
              </view>
            </view>
          </view>
        </view>
        <view class="orbit__pedestal" aria-hidden="true" />
      </view>
    </block>

    <view wx:else class="orbit__viewer">
      <swiper
        class="orbit__swiper"
        current="{{expandedIndex}}"

灵祺星选达人撮合小程序软件 V1.0
第 24 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
        circular="{{faceCount > 1}}"
        duration="{{280}}"
        bindchange="onSwiperChange"
      >
        <swiper-item wx:for="{{faces}}" wx:key="src" wx:for-index="slideIndex">
          <view class="orbit__slide">
            <image
              class="orbit__viewer-img"
              src="{{item.hdSrc}}"
              mode="widthFix"
              data-slide-index="{{slideIndex}}"
              show-menu-by-longpress="{{true}}"
              binderror="onImgError"
            />
          </view>
        </swiper-item>
      </swiper>
      <view class="orbit__viewer-bar">
        <view class="orbit__viewer-close" catchtap="onCloseExpand">收起</view>
      </view>
    </view>
  </view>
</view>


/* ===== FILE: 灵祺达人撮合小程序/components/login-orbit-wall/index.wxss ===== */
.orbit {
  width: 100%;
  box-sizing: border-box;
}

.orbit__hint {
  text-align: center;
  padding: 6rpx 0 10rpx;
}

.orbit__hint-txt {
  font-size: 22rpx;
  color: var(--lq-text-secondary, #6b7280);
  letter-spacing: 1rpx;
}

.orbit__stage {
  position: relative;
  width: 100%;
  height: 540rpx;
  box-sizing: border-box;
}

.orbit__stage--expanded {

灵祺星选达人撮合小程序软件 V1.0
第 25 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  height: 600rpx;
}

.orbit__ring-wrap {
  position: relative;
  width: 100%;
  height: 100%;
  perspective: 1280rpx;
  perspective-origin: 50% 40%;
}

.orbit__ring {
  position: absolute;
  left: 50%;
  top: 44%;
  width: 0;
  height: 0;
  transform-style: preserve-3d;
  transition: none;
  will-change: transform;
  backface-visibility: hidden;
}

.orbit__ring--snap {
  transition: transform 0.45s cubic-bezier(0.22, 1, 0.36, 1);
}

.orbit__face {
  position: absolute;
  left: 0;
  top: 0;
  width: 228rpx;
  height: 286rpx;
  margin-left: -114rpx;
  margin-top: -143rpx;
  transform-style: preserve-3d;
  backface-visibility: visible;
}

.orbit__card {
  width: 100%;
  height: 100%;
  border-radius: 26rpx;
  overflow: hidden;
  border: 4rpx solid rgba(255, 255, 255, 0.95);
  box-shadow:
    0 20rpx 44rpx rgba(99, 102, 241, 0.26),
    0 6rpx 16rpx rgba(15, 23, 42, 0.1);
  background: #fff;
  position: relative;

灵祺星选达人撮合小程序软件 V1.0
第 26 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
}

.orbit__img {
  width: 100%;
  height: 100%;
  display: block;
  pointer-events: none;
}

.orbit__shine {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  height: 42%;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.32) 0%, transparent 100%);
  pointer-events: none;
}

.orbit__zoom-hint {
  position: absolute;
  right: 10rpx;
  bottom: 10rpx;
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: rgba(15, 23, 42, 0.42);
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}

.orbit__zoom-icon {
  font-size: 26rpx;
  color: #fff;
  line-height: 1;
}

.orbit__pedestal {
  position: absolute;
  left: 50%;
  bottom: 8rpx;
  width: 520rpx;
  height: 72rpx;
  margin-left: -260rpx;
  border-radius: 50%;
  background: radial-gradient(
    ellipse at center,
    rgba(139, 147, 255, 0.32) 0%,

灵祺星选达人撮合小程序软件 V1.0
第 27 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
    rgba(139, 147, 255, 0.08) 55%,
    transparent 72%
  );
  transform: rotateX(72deg);
}

/* 页内大图：占满环墙区域（红框范围） */
.orbit__viewer {
  width: 100%;
  height: 100%;
  padding: 0 12rpx 8rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
}

.orbit__swiper {
  flex: 1;
  width: 100%;
  min-height: 0;
  border-radius: 28rpx;
  overflow: hidden;
  background: linear-gradient(165deg, #1e1b2e 0%, #2d2640 48%, #1a1730 100%);
  border: 4rpx solid rgba(216, 200, 255, 0.55);
  box-shadow:
    0 20rpx 48rpx rgba(99, 102, 241, 0.2),
    inset 0 0 0 1rpx rgba(255, 255, 255, 0.08);
}

.orbit__slide {
  width: 100%;
  min-height: 100%;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  box-sizing: border-box;
  padding: 16rpx 20rpx 8rpx;
  overflow-y: auto;
}

.orbit__viewer-img {
  width: 100%;
  height: auto;
  max-height: 520rpx;
  display: block;
}

.orbit__viewer-bar {
  flex-shrink: 0;
  display: flex;

灵祺星选达人撮合小程序软件 V1.0
第 28 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
  justify-content: center;
  padding: 14rpx 0 4rpx;
}

.orbit__viewer-close {
  padding: 10rpx 36rpx;
  font-size: 24rpx;
  font-weight: 600;
  color: var(--lq-primary-deep, #4f46e5);
  background: rgba(255, 255, 255, 0.92);
  border: 2rpx solid rgba(216, 200, 255, 0.65);
  border-radius: 999rpx;
  box-shadow: 0 6rpx 16rpx rgba(139, 147, 255, 0.12);
}


/* ===== FILE: 灵祺达人撮合小程序/custom-tab-bar/index.js ===== */
const userProfile = require('../utils/userProfile.js')
const { getTabList } = require('../utils/tabBarConfig.js')
const chatBadgeWatcher = require('../utils/chatBadgeWatcher.js')

Component({
  data: {
    selected: 0,
    hidden: false,
    hasCenterFab: false,
    chatBadge: 0,
    list: getTabList(userProfile.readIdentity()),
  },
  lifetimes: {
    attached() {
      this.applyIdentityLayout()
      chatBadgeWatcher.syncBarFromGlobal()
      void chatBadgeWatcher.refreshNow()
    },
  },
  pageLifetimes: {
    show() {
      this.applyIdentityLayout()
      chatBadgeWatcher.syncBarFromGlobal()
      void chatBadgeWatcher.refreshNow()
    },
  },
  methods: {
    applyIdentityLayout() {
      const list = getTabList(userProfile.readIdentity())
      const hasCenterFab = list.some((item) => item && item.center)
      const cur = this.data.list || []
      const same =
        cur.length === list.length && cur.every((item, i) => item.pagePath === list[i].pagePath)

灵祺星选达人撮合小程序软件 V1.0
第 29 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
      if (!same || this.data.hasCenterFab !== hasCenterFab) {
        this.setData({ list, hasCenterFab })
      }
    },
    switchTab(e) {
      const idx = Number(e.currentTarget.dataset.index)
      const item = this.data.list[idx]
      if (!item) return
      wx.switchTab({ url: item.pagePath })
    },
  },
})


/* ===== FILE: 灵祺达人撮合小程序/custom-tab-bar/index.json ===== */
{
  "component": true,
  "styleIsolation": "apply-shared"
}


/* ===== FILE: 灵祺达人撮合小程序/custom-tab-bar/index.wxml ===== */
<view class="tab-bar {{hasCenterFab ? 'tab-bar--fab' : ''}}" wx:if="{{!hidden}}">
  <view
    wx:for="{{list}}"
    wx:key="pagePath"
    class="tab-item {{item.center ? 'tab-item--center' : ''}} {{selected === index ? 'active' : ''}}"
    data-index="{{index}}"
    bindtap="switchTab"
  >
    <block wx:if="{{item.center}}">
      <view class="fab-btn">
        <view class="fab-btn-inner">
          <view class="fab-line fab-line--h"></view>
          <view class="fab-line fab-line--v"></view>
        </view>
      </view>
      <text class="tab-text tab-text--fab">{{item.text}}</text>
    </block>
    <block wx:else>
      <view class="tab-icon-wrap">
        <view class="tab-icon tab-icon-{{item.icon}} {{selected === index ? 'is-active' : ''}}">
          <block wx:if="{{item.icon === 'home'}}">
            <view class="ico ico-home">
              <view class="ico-home-roof"></view>
              <view class="ico-home-body">
                <view class="ico-home-door"></view>
              </view>
            </view>
          </block>

灵祺星选达人撮合小程序软件 V1.0
第 30 页 / 共 30 页（登记摘录）
————————————————————————————————————————————————————————————
          <block wx:elif="{{item.icon === 'star'}}">
            <view class="ico ico-star"></view>
            <view class="tab-ai-badge" wx:if="{{item.aiBadge}}">AI</view>
          </block>
          <block wx:elif="{{item.icon === 'chat'}}">
            <view class="ico ico-chat">
              <view class="ico-chat-tail"></view>
            </view>
            <view class="tab-badge" wx:if="{{chatBadge > 0}}">{{chatBadge > 99 ? '99+' : chatBadge}}</view>
          </block>
          <block wx:else>
            <view class="ico ico-user">
              <view class="ico-user-head"></view>
              <view class="ico-user-shoulder"></view>
            </view>
          </block>
        </view>
      </view>
      <text class="tab-text">{{item.text}}</text>
    </block>
  </view>
</view>


/* ===== FILE: 灵祺达人撮合小程序/custom-tab-bar/index.wxss ===== */
@import '../styles/theme.wxss';

.tab-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: 6rpx 12rpx calc(6rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(12px);
  border-top: 1rpx solid var(--lq-border, rgba(210, 212, 230, 0.88));
  box-shadow: 0 -6rpx 24rpx rgba(90, 96, 140, 0.06);
  z-index: 999;
  height: calc(96rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
}

.tab-bar--fab {
  align-items: flex-end;
  height: calc(108rpx + env(safe-area-inset-bottom));
  padding-top: 0;
}
