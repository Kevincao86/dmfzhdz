---
description: 灵祺达人撮合小程序仅走 ECS，禁止 Supabase 云与 Vercel
alwaysApply: true
---

# 小程序数据面（ECS only）

- **唯一 API**：`https://mofangdianai.com/erp-api` → ECS `meoo-auth-api` :3001。
- **禁止**：`*.supabase.co` 云端、`cs.mofangdianai.com` / Vercel 代拉、`api.mofangdianai.com` 子域（除非用户明确要求）。
- **数据**：ECS Postgres + PostgREST `127.0.0.1:8888`；环境变量名 `SUPABASE_*` 仅为历史命名。
- **代码路径**：Git 在 `web版/merchant-erp`；ECS `systemd WorkingDirectory` 须与 `bash scripts/ecs-verify-auth-api-path.sh` 一致（勿只更新 `web/merchant-erp` 旧目录）。
- **小程序网络层**：`utils/ecsApi.js`；登录 `POST /api/meoo-ops-mp-auth` JSON body，不用 GET 长 query。
- **体验版构建号**：`mp-20260604-ecs-only`（见 `config.release.js` MP_BUILD_ID）。
