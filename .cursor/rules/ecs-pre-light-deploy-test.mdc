---
description: 部署轻量前必须先本机冒烟测试 auth-api，通过后才能上传
alwaysApply: true
---

# 轻量部署前本机测试（写死）

**任何**需要部署 **轻量**（`139.196.42.5` / `meoo-auth-api` / `/erp-api/*`）时，Agent **必须先**在本机跑冒烟测试，**通过后才能** git push / SSH 上传 / 执行部署脚本。

## 固定顺序（不可跳过、不可颠倒）

1. **本机冒烟**（必跑）：

```bash
bash scripts/ecs-pre-light-deploy-test.sh
```

2. **仅当 exit 0** 后，才允许部署轻量：

```bash
bash scripts/ecs-deploy-light-safe.sh
# 或
bash scripts/ecs-deploy-light-and-cs-remote.sh   # 含轻量时同样须先跑步骤 1
```

3. **部署后只读验收**（不写库）：

```bash
curl -sS "http://139.196.42.5/erp-api/meoo-erp-api-health"
```

## 测试未通过时

- **禁止**部署轻量、禁止 SSH 拉代码到轻量
- 先修代码/依赖，再重跑 `ecs-pre-light-deploy-test.sh`
- 向用户说明失败日志中的崩溃原因（import 错误、语法错误、缺依赖等）

## 适用范围

- 改了 `web版/merchant-erp/api/**`、`scripts/ecs-auth-api-server.ts`、商家管理后台 API 等 auth-api 路由
- 用户说「部署轻量」「上轻量」「重启 auth-api」
- `ecs-deploy-light-safe.sh`、`ecs-deploy-light-and-cs-remote.sh`（`DEPLOY_LIGHT=1`）

## Agent 回复格式

完成修改且测试通过后，结尾须含：

- `→ 本机冒烟已通过`
- `→ 部署 **轻量**`（或「先轻量再新ECS」）

测试未通过时只写修复项，**不得**写「→ 部署轻量」。
