---
description: 部署目标写死：只说「轻量」或「新ECS」，禁止笼统说 ECS
alwaysApply: true
---

# 部署目标（写死）

**禁止**对用户说「部署 ECS」「上 ECS」「ECS 部署」等笼统说法。必须明确说 **轻量** 或 **新ECS**（可附带 IP）。

## 两台机器

| 名称 | IP | 跑什么 |
|------|-----|--------|
| **轻量** | `139.196.42.5` | `meoo-auth-api`（:3001）、Postgres、PostgREST；`mofangdianai.com/erp-api`；小程序唯一 API |
| **新ECS** | `8.160.173.236` | 仅静态 Web + Nginx；`cs` / `admin` / `fws` / `dr` 子域；API 反代到轻量 |

## 改什么 → 部署哪里

| 改动类型 | 部署 |
|----------|------|
| `web版/merchant-erp/api/**`、auth-api 路由、云剪/商品方案/AI 对话等后端 | **轻量** |
| 小程序后端接口（`/erp-api/*`） | **轻量** |
| 商家 ERP 前端（`web版/merchant-erp/src/**` 需重新打包） | **新ECS**（`cs.mofangdianai.com`） |
| 运营管控台前端 | **新ECS**（`admin.mofangdianai.com`） |
| 服务商 ERP 前端 | **新ECS**（`fws.mofangdianai.com`） |
| 履约后台前端 | **新ECS**（`dr.mofangdianai.com`） |
| 根域 `mofangdianai.com` 单机全栈（少见） | **轻量** |

**原则**：`/erp-api/*` 永远在 **轻量**；只改后端 → 只部署 **轻量**；改了前端 bundle → 还要部署 **新ECS**。

## 固定命令

**轻量** — 须先本机冒烟（写死，见 `ecs-pre-light-deploy-test.mdc`）：

```bash
bash scripts/ecs-pre-light-deploy-test.sh   # 必须先 exit 0
bash scripts/ecs-deploy-light-safe.sh       # 再部署
```

**轻量**（SSH 到轻量上执行）：

```bash
cd ~/app && bash scripts/ecs-git-pull-gitee.sh && bash scripts/ecs-deploy-auth-api.sh
```

**新ECS** — 商家 / 服务商前端（**写死：本机构建**，见 `ecs-new-ecs-local-build-only.mdc`）：

```bash
bash scripts/ecs-deploy-cs-web-local-build.sh          # cs
bash scripts/ecs-deploy-fws-web-local-build.sh         # fws
bash scripts/ecs-deploy-dr-web-local-build.sh          # dr（@merchant=CS）
bash scripts/ecs-deploy-new-ecs-web-local-build.sh     # cs+fws
```

**禁止**在新ECS（`8.160.173.236`）上直接 `npm run build`（2G 会 OOM 卡死）。远程仅允许 `SKIP_BUILD=1`。

**新ECS** — 运营台（`admin`，同样优先本机构建再 `SKIP_BUILD=1`）：

```bash
# 本机构建运营台 dist 后，再 SSH 新ECS：
cd ~/app && bash scripts/ecs-git-pull-gitee.sh && SKIP_BUILD=1 bash scripts/ecs-deploy-ops-admin-web.sh
```

**两台都要**（本机一键；含轻量 + 本机构建 cs）：

```bash
bash scripts/ecs-deploy-light-and-cs-remote.sh
```

## Agent 回复格式

完成代码修改后，结尾必须写一行，例如：

- `→ 部署 **轻量**`
- `→ 部署 **新ECS**（商家 cs）`
- `→ 先 **轻量**，再 **新ECS**（商家 cs）`

不得只写「部署到 ECS」。
