<view class="page">
  <view class="status-bar">
    <text class="status-title">{{humanMode ? '人工客服已接入' : '联系客服 · 智能助手'}}</text>
    <text class="status-sub">{{statusSub}}</text>
  </view>

  <scroll-view
    scroll-y
    class="chat-scroll"
    scroll-into-view="{{scrollTo}}"
    scroll-with-animation
  >
    <view
      wx:for="{{messages}}"
      wx:key="id"
      id="msg-{{item.id}}"
      class="bubble-row {{item.role === 'user' ? 'mine' : 'their'}}"
    >
      <view class="bubble bubble-{{item.role}}">
        <text class="bubble-text">{{item.text}}</text>
        <text class="bubble-at">{{item.at}}</text>
      </view>
    </view>
    <view class="scroll-pad"></view>
  </scroll-view>

  <view class="composer">
    <button
      wx:if="{{showHumanBtn && !humanMode}}"
      class="human-btn"
      bindtap="onRequestHuman"
      loading="{{connecting}}"
      disabled="{{connecting || !ready}}"
    >
      人工服务
    </button>
    <view class="input-row">
      <input
        class="chat-input"
        placeholder="{{humanMode ? '向人工客服描述问题…' : '输入问题；输入「人工服务」可转人工'}}"
        value="{{input}}"
        bindinput="onInput"
        confirm-type="send"
        bindconfirm="onSend"
        disabled="{{!ready}}"
      />
      <button class="send-btn" bindtap="onSend" disabled="{{!ready || !input}}">发送</button>
    </view>
  </view>
</view>
