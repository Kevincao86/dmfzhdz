# 复制为 .env.local（勿提交）

# 注册表已在本项目 Vite 内直接读写（项目根 .meoo-dev-sync），无需再配置 ERP 代理。

# 在线客服 ws 覆盖（默认本控制台 dev 同源 /__meoo_support_online，由 vite-plugins/supportOnlineWs 提供）
# VITE_SUPPORT_RELAY_WS=ws://127.0.0.1:5174/__meoo_support_online

# 管理端 OpenAPI 根路径（与商家端共用同一网关时填相同域名）；未配置时页面使用内置演示数据
# VITE_OPS_API_BASE_URL=https://api.example.com

# —— Supabase：客户管理「手动创建」时先开通 Auth + tenants——
# 云端：部署 Edge Function `provision-tenant` 并配置密钥。
# 本地：项目根执行 `npm run supabase:start`，再 `npm run supabase:functions:serve`，见各 .env.example。
# 云端：
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_ANON_KEY=eyJ...（仅 dev 代理，勿提交 service_role）
# 客户管理列出 Supabase 租户（仅 Vite 服务端读取，勿加 VITE_ 前缀）：
# SUPABASE_SERVICE_ROLE_KEY=<Dashboard → Project Settings → API → service_role>
# 本地：
# VITE_SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=<npm run supabase:status 输出的 anon key>
# MEOO_PROVISION_SECRET=与 supabase/.env 中一致
# VITE_SUPABASE_TENANT_EMAIL_DOMAIN=users.meoo.test（须与 Edge env TENANT_EMAIL_DOMAIN 一致）
# 注意：本地 API 为 127.0.0.1:54321 时，请勿在 .env.local 填写「云端项目」的 SUPABASE_SERVICE_ROLE_KEY（会导致客户管理列租户失败）。
# dev 插件会自动使用 Supabase CLI 内置 demo service_role JWT。若必须使用 supabase status 打印的密钥，再加：
# SUPABASE_LOCAL_USE_PRINTED_SERVICE_ROLE=1
# SUPABASE_SERVICE_ROLE_KEY=<supabase status 中的 service_role key>
