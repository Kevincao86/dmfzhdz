<scroll-view scroll-y class="page-func-v2" enhanced show-scrollbar="{{false}}" style="padding-bottom:var(--tab-bar-pad)">
  <view class="func-top">
    <view>
      <text class="func-h1">{{hubTitle}}</text>
      <text class="func-p">{{hubSub}}</text>
    </view>
    <image class="func-illus-img" src="/images/func-hero-illus.svg" mode="aspectFit" />
  </view>
  <block wx:for="{{sections}}" wx:key="id">
    <navigator wx:if="{{item.layout === 'row'}}" url="{{item.items[0].url}}" class="sec-card-v2">
      <view class="sec-row-v2">
        <view class="ibox ibox-{{item.tone}} ibox-sm">
          <image class="func-ic-img func-ic-sm" src="{{item.sectionIconSrc}}" mode="aspectFit" />
        </view>
        <text class="sec-row-title">{{item.title}}</text>
        <text class="sec-row-desc">{{item.rowDesc}}</text>
        <text class="chev">›</text>
      </view>
    </navigator>
    <view wx:else class="sec-card-v2">
      <view class="sec-head-v2">
        <view class="ibox ibox-{{item.tone}} ibox-sm">
          <image class="func-ic-img func-ic-sm" src="{{item.sectionIconSrc}}" mode="aspectFit" />
        </view>
        <text>{{item.title}}</text>
        <text class="chev">›</text>
      </view>
      <view class="grid-v2 grid-v2-{{item.cols}}">
        <navigator
          wx:for="{{item.items}}"
          wx:for-item="cell"
          wx:for-index="idx"
          wx:key="title"
          url="{{cell.url}}"
          class="cell-v2 {{idx % item.cols !== item.cols - 1 ? 'cell-v2-divide' : ''}}"
        >
          <view class="ibox ibox-{{item.tone}} ibox-sm">
            <image class="func-ic-img func-ic-sm" src="{{cell.iconSrc}}" mode="aspectFit" />
          </view>
          <view class="cell-text">
            <text class="cell-strong">{{cell.title}}</text>
            <text class="cell-span">{{cell.desc}}</text>
          </view>
        </navigator>
      </view>
    </view>
  </block>
</scroll-view>
