# 复制为 `.env.local`（已被 .gitignore 忽略）后按需填写。
#
# 若 Key 只配在 Vercel、希望本地 dev 与原环境一致：本机需要先「拉取」到磁盘（Vercel ≠ 本地自动同步）。
# 在项目根已与 Vercel 关联的前提下，可到本目录执行（需已 npm i / 或使用 npx）：
#   npx vercel env pull .env.vercel.development --environment development --yes
# 再在编辑器里打开 .env.vercel.development 与当前的 .env.local，把需要的行合并进来（或直接覆盖后补回下文 MEOO_ 开关）。
#
# ——商家小程序调试「跳过登录」时，智能体网关 /api/meoo-ai-chat 需要：——
# 1）本开关（仅本地，上架与生产禁用）
MEOO_AI_CHAT_ALLOW_UNAUTHENTICATED=1
#
# 2）至少配置一项对话/智能体可用的 Key（任选）
# MERCHANT_AI_QWEN_KEY=
# DASHSCOPE_API_KEY=
# MERCHANT_AI_DOUBAO_KEY=
# ARK_API_KEY=
# TOKENMIX_API_KEY=
